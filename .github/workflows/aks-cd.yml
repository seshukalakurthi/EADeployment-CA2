name: AKS CD
# Deploying Pods to AKS

on:
  workflow_run:
    workflows: ["ACR CI"]
    types:
      - completed

  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    if: >
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success') || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest

    environment:
      name: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download image tag artifact
        uses: actions/download-artifact@v4
        with:
          name: image-tag

      - name: Read image tag
        run: |
          IMAGE_TAG=$(cat image_tag.txt)
          echo "IMAGE_TAG=$IMAGE_TAG" >> $GITHUB_ENV

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Get AKS Credentials
        run: |
          az aks get-credentials \
            --resource-group ${{ secrets.RESOURCE_GROUP }} \
            --name ${{ secrets.AKS_CLUSTER }} \
            --overwrite-existing

      - name: Create Namespace
        run: |
          kubectl create namespace eadeploy-ca2 --dry-run=client -o yaml | kubectl apply -f -

      - name: Apply Kubernetes Manifests
        run: |
          kubectl apply -f k8s/mongodb/ -n eadeploy-ca2
          kubectl apply -f k8s/backend/ -n eadeploy-ca2
          kubectl apply -f k8s/frontend/ -n eadeploy-ca2

      - name: Wait for MongoDB
        run: kubectl rollout status deployment/mongodb -n eadeploy-ca2

      - name: Deploy Backend
        run:  |
            kubectl set image deployment/backend \
            backend=eadeployca2receiperegistry.azurecr.io/eadca2_backend:${IMAGE_TAG} \
            -n eadeploy-ca2

      - name: Wait for Backend
        run: kubectl rollout status deployment/backend -n eadeploy-ca2

      - name: Deploy Frontend
        run: |
            kubectl set image deployment/frontend \
            frontend=eadeployca2receiperegistry.azurecr.io/eadca2_frontend:${IMAGE_TAG} \
            -n eadeploy-ca2
      - name: Wait for Frontend
        run: kubectl rollout status deployment/frontend -n eadeploy-ca2

      - name: Verify
        run: |
          kubectl get pods -n eadeploy-ca2
          kubectl get svc -n eadeploy-ca2
          kubectl get ingress -n eadeploy-ca2
